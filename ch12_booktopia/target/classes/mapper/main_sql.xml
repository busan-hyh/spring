<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.main_sql">
	<select id="SELECT_BESTSELLER" resultType="kr.co.booktopia.vo.ListVO">
		SELECT * FROM `BT_SHOP_GOODS` AS L 
		INNER JOIN `BT_SHOP_GOODS_IMAGE` AS I
		ON L.GOODS_ID = I.GOODS_ID 
		WHERE L.GOODS_STATUS='bestseller' 
		GROUP BY L.GOODS_ID 
		ORDER BY L.GOODS_CREDATE DESC
		LIMIT 12 
	</select>
	<select id="SELECT_NEW" resultType="kr.co.booktopia.vo.ListVO">
		SELECT * FROM `BT_SHOP_GOODS` AS L 
		INNER JOIN `BT_SHOP_GOODS_IMAGE` AS I
		ON L.GOODS_ID = I.GOODS_ID 
		WHERE L.GOODS_STATUS='newbook' 
		GROUP BY L.GOODS_ID 
		ORDER BY L.GOODS_CREDATE DESC
		LIMIT 4 
	</select>
	<select id="SELECT_STEADYSELLER" resultType="kr.co.booktopia.vo.ListVO">
		SELECT * FROM `BT_SHOP_GOODS` AS L 
		INNER JOIN `BT_SHOP_GOODS_IMAGE` AS I
		ON L.GOODS_ID = I.GOODS_ID 
		WHERE L.GOODS_STATUS='steadyseller' 
		GROUP BY L.GOODS_ID 
		ORDER BY L.GOODS_CREDATE DESC
		LIMIT 4 
	</select>
</mapper>